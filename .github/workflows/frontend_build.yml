name: 🚀 Build et déploiement du frontend React

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Récupération du code
        uses: actions/checkout@v4

      - name: 🧰 Installation de Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 📦 Installation des dépendances
        run: |
          cd admin-dashboard
          npm ci

      - name: 🏗️ Build du frontend (ignore les erreurs TypeScript)
        run: |
          cd admin-dashboard
          npx tsc --noEmit || true
          npm run build --if-present

      - name: 📂 Vérification du build généré
        run: |
          cd admin-dashboard
          ls -la dist || ls -la build

      # 🚀 Étape suivante (placeholder) : déploiement sur VPS
      - name: 🚀 (Préparation) Déploiement sur VPS
        run: echo "Étape de déploiement à venir (rsync + PM2)"
